{
  "name": "4-yoga-2",
  "version": "1.0.0",
  "description": "Yoga try to remember",
  "main": "index.js",
  "scripts": {
    "dev": "dead-server --port=3000 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Povilas",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.6"
  }
}
